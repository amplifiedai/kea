use Rand
use Test

fn sample() -[Rand]> Int
  Rand.seed(1234)
  Rand.int()

test "Rand.seed_and_int_smoke"
  let x = handle sample()
    Rand.seed(seed) -> resume ()
    Rand.int() -> resume 7
  Test.assert(x == x)
