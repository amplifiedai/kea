use Order

trait Ord a
  --| Compare two values.
  --|
  --|   Ord.compare(1, 2)   -- => Less
  --|   Ord.compare(2, 2)   -- => Equal
  fn compare(x: a, y: a) -> Ordering

--| Int ordering implementation for Ord.
--|
--|   Ord.compare(1, 2)   -- => Less
--|   Ord.compare(3, 1)   -- => Greater
fn compare(x: Int, y: Int) -> Ordering
  if x < y
    Order.Less
  else if x > y
    Order.Greater
  else
    Order.Equal
