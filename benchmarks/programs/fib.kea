-- Naive recursive fibonacci — measures raw call overhead.
-- fib(40) = 102334155

fn fib(n: Int) -> Int
  if n < 2
    n
  else
    fib(n - 1) + fib(n - 2)

fn main() -> Int
  let result = fib(40)
  if result == 102334155
    0
  else
    1
